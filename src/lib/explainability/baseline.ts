/**
 * Stable baseline contract/version markers.
 *
 * - CERTAINTY_CONTRACT_VERSION:
 *   version of explainability output contract (CertaintySummary fields/semantics).
 * - MODEL_BASELINE_VERSION:
 *   version of current model baseline used by realtime pipeline/debug scripts.
 */
export const CERTAINTY_CONTRACT_VERSION = 'v1' as const;
export const MODEL_BASELINE_VERSION = 'shanghai-rule-v1' as const;

